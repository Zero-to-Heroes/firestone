// /* eslint-disable @typescript-eslint/no-use-before-define */
// import 'jest-extended';
// import { gameStateBuilder } from '../../final-game-state-builder';
// import pluginEvents from './plugin-events.json';

// describe('Kara Stamper hero power removes card from her hand', () => {
// 	test('cards discarded by her hero power are not in hand anymore', async () => {
// 		const gameState = await gameStateBuilder(pluginEvents, null);

// 		// DALA_BOSS_74p is the hero power
// 		expect(gameState.opponentDeck.hand.map(card => card.entityId)).not.toContain(1);
// 	});
// });
