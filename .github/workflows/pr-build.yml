name: PR Build Check

on:
    pull_request:
        branches:
            - '*'

jobs:
    build:
        runs-on: ubuntu-latest
        env:
            # ow-electron postinstall downloads platform binaries from Overwolf's CDN; Linux
            # artifacts are not published there. CI only compiles the Overwolf app.
            ELECTRON_SKIP_BINARY_DOWNLOAD: 1
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
              with:
                  fetch-depth: 0

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: '20'
                  cache: 'npm'

            - name: Install dependencies
              run: npm ci

            - name: Build with production configuration
              run: npx nx build --configuration=production
              env:
                  NODE_ENV: production
                  NODE_OPTIONS: --max-old-space-size=12288
