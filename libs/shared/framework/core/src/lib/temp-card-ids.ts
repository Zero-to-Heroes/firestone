/**
 * Placeholder card ids for patch 35.4 (Mend / CATA event) until @firestone-hs/reference-data lists them.
 * Replace with CardIds when available; values use TEMP_ prefix for bulk find-replace.
 */
export enum TempCardIds {}
