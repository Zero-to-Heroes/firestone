/* eslint-disable no-mixed-spaces-and-tabs */
/**
 * Transformthe Key (JAIL_319t)
 * Refresh your options. (20% chance to take 5 damage!)
 */
import { CardIds } from '@firestone-hs/reference-data';
import { TempCardIds } from '@firestone/shared/framework/core';
import { Card } from './_card.type';

export const TransformtheKey: Card = {
	cardIds: [TempCardIds.TransformtheKey_JAIL_319t as unknown as CardIds],
};
