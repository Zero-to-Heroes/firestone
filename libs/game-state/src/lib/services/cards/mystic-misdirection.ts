/* eslint-disable no-mixed-spaces-and-tabs */
/**
 * Mystic Misdirection (JAIL_315)
 * Secret: When an enemy minion attacks, transform it into a 1/1 Sheep.
 */
import { CardIds } from '@firestone-hs/reference-data';
import { TempCardIds } from '@firestone/shared/framework/core';
import { Card } from './_card.type';

export const MysticMisdirection: Card = {
	cardIds: [TempCardIds.MysticMisdirection_JAIL_315 as unknown as CardIds],
};
