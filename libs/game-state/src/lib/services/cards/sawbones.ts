/* eslint-disable no-mixed-spaces-and-tabs */
/**
 * Sawbones (JAIL_444)
 * Prepare. Battlecry: Destroy all your other minions. Draw a card and refresh a Mana for each one destroyed.
 */
import { CardIds } from '@firestone-hs/reference-data';
import { TempCardIds } from '@firestone/shared/framework/core';
import { Card } from './_card.type';

export const Sawbones: Card = {
	cardIds: [TempCardIds.Sawbones_JAIL_444 as unknown as CardIds],
};
