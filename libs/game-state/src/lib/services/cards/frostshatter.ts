/* eslint-disable no-mixed-spaces-and-tabs */
/**
 * Frostshatter (JAIL_803)
 * Freeze an enemy. Draw 2 cards. (Cast 3 spells to turn into a minion!)
 */
import { CardIds } from '@firestone-hs/reference-data';
import { TempCardIds } from '@firestone/shared/framework/core';
import { Card } from './_card.type';

export const Frostshatter: Card = {
	cardIds: [TempCardIds.Frostshatter_JAIL_803 as unknown as CardIds],
};
