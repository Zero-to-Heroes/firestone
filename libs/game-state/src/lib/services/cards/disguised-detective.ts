/* eslint-disable no-mixed-spaces-and-tabs */
/**
 * Disguised Detective (JAIL_452)
 * Can be played on either side. Overload that player for (2).
 */
import { CardIds } from '@firestone-hs/reference-data';

import { Card } from './_card.type';

export const DisguisedDetective: Card = {
	cardIds: [CardIds.DisguisedDetective_JAIL_452],
};
