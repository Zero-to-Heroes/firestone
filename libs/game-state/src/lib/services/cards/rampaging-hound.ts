/* eslint-disable no-mixed-spaces-and-tabs */
/**
 * Rampaging Hound (JAIL_435)
 * Prepare. Battlecry: Force all enemy minions to attack this.
 */
import { CardIds } from '@firestone-hs/reference-data';
import { TempCardIds } from '@firestone/shared/framework/core';
import { Card } from './_card.type';

export const RampagingHound: Card = {
	cardIds: [TempCardIds.RampagingHound_JAIL_435 as unknown as CardIds],
};
