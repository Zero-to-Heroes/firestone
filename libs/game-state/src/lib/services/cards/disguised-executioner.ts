/* eslint-disable no-mixed-spaces-and-tabs */
/**
 * Disguised Executioner (JAIL_461)
 * Can be played on either side. Battlecry: Destroy a random adjacent minion.
 */
import { CardIds } from '@firestone-hs/reference-data';

import { Card } from './_card.type';

export const DisguisedExecutioner: Card = {
	cardIds: [CardIds.DisguisedExecutioner_JAIL_461],
};
