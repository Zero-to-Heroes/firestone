/* eslint-disable no-mixed-spaces-and-tabs */
/**
 * IMPFERNAL (JAIL_398)
 * Deathrattle: Deal 3 damage to all other characters. (Also triggers in hand or deck.)
 */
import { CardIds } from '@firestone-hs/reference-data';
import { TempCardIds } from '@firestone/shared/framework/core';
import { Card } from './_card.type';

export const IMPFERNAL: Card = {
	cardIds: [TempCardIds.IMPFERNAL_JAIL_398 as unknown as CardIds],
};
