/* eslint-disable no-mixed-spaces-and-tabs */
/**
 * Captured Archmage (JAIL_974)
 * Deathrattle: If you had 4 other Captured Archmages die this game, cast 'Fireball' at a random enemy.
 */
import { CardIds } from '@firestone-hs/reference-data';
import { TempCardIds } from '@firestone/shared/framework/core';
import { Card } from './_card.type';

export const CapturedArchmage: Card = {
	cardIds: [TempCardIds.CapturedArchmage_JAIL_974 as unknown as CardIds],
};
